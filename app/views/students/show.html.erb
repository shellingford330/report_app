<% provide(:title, @student.name)%>
<div class="container">

	<p>
		<strong>学年</strong>
		<%= @student.grade %>
	</p>
	<p>
		<strong>生徒名</strong>
		<%= @student.name %>
	</p>
	<p>
		<strong>メールアドレス</strong>
		<%= @student.email %>
	</p>
</div>