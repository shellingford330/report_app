<% provide(:title, "生徒作成")%>
<% provide(:btn_text, "作成する")%>
<div class="container  mt-3">
	<div class="row">
		<div class="col-md-9 mx-auto">
			<div class="card bg-brown shadow">
				<div class="card-header">
					<h2><i class="fas fa-user-plus"></i> 生徒登録</h2>
				</div>
				<div class="card-body">
					<%= render 'form', student: @student %>
				</div>
			</div>
		</div>
	</div>
</div>