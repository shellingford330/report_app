<%= @receiver %> 様

<%= @receiver %>さんへ<%= @sender %>から返信がありました。
ご確認される場合は、以下のリンクをクリックして下さい。
<% if @kind_of_doc == "お問い合せ" %>
	<%= contact_url(@doc) %>
<% else %>
	<%= report_url(@doc) %>
<% end %>

<%= render 'zyuku' %>
