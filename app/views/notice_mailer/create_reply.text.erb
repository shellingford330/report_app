<%= @receiver.name %> 様

<%= @receiver.name %>さんへ<%= @sender.name %>から返信がありました。
ご確認される場合は、以下のリンクをクリックして下さい。
<% if @paper_name == "お問い合せ" %>
	<%= contact_url(@paper) %>
<% else %>
	<%= report_url(@paper) %>
<% end %>

<%= render 'zyuku' %>
